const express = require('express');

const router = express.Router();

/**
 * MindTrain User Routes - Unified Nested Schema API
 * 
 * All unified APIs have been removed.
 * Use legacy APIs in alarmProfile.routes.js instead:
 * - POST /api/mindtrain/create-alarm-profile
 * - GET /api/mindtrain/get-alarm-profiles
 * - DELETE /api/mindtrain/alarm-profiles/:profileId
 * 
 * All routes use the unified nested schema (MindTrainUser model) under the hood.
 */

// All unified API routes have been removed - using legacy APIs only

module.exports = router;

